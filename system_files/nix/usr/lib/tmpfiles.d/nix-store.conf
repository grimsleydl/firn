# Mount point for bind mount (content lives in /var/lib/nix-store)
d /nix 0755 root root -
# Persistent store directory (populated at build time from Fedora's /nix)
d /var/lib/nix-store 0755 root root -
